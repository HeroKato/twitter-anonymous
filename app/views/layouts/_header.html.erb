<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="container">
    <%= link_to "TwitterAnonymous", '#', id: "logo" %>
    <nav>
      <ul class="nav navbar-nav navbar-right">
        <li>
          <% if logged_in? %>
            <%= link_to "Log Out", logout_path %>
          <% else %>
            <%= link_to "Log in with Twitter", "/auth/twitter" %>
          <% end %>
        </li>
      </ul>
    </nav>
  </div>
</header>